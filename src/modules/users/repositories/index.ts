import { getRepository, Repository } from 'typeorm'
import { User } from '../infra/typeorm/entities/User'

// export declare type UserRepositoryType = Repository<User>
// export const getUserRepository = () => getRepository(User)
